# Copy this to .env and set values, or set in PowerShell session
# Note: The app reads environment variables directly; it does not parse .env files.
JIRA_BASE_URL="https://onejira.verizon.com"
JIRA_EMAIL="marieswari.palanimarimuthu@verizon.com"
JIRA_API_TOKEN="YOUR_JIRA_API_TOKEN_HERE"
# Set to 1 to use mock data instead of Jira API
#JIRA_MOCK=1
$env:JIRA_API_VERSION = "2"   